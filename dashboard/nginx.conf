server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # proxy API calls to sidecar — SIDECAR_UPSTREAM replaced at container start
    location /api/ {
        proxy_pass ${SIDECAR_UPSTREAM};
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # injected here so the token never reaches the browser;
        # empty value (INMEM_TOKEN unset) makes nginx omit the header
        proxy_set_header x-inmem-token "${INMEM_TOKEN}";
        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
    }
}
